{# empty Twig template #}
<html>
    <head>
        <title>Crud tutorial</title>
        <link rel="stylesheet" href="{{ absolute_url(asset('css/bootstrap.css')) }}" />
        <script src="{{ absolute_url(asset('js/bootstrap.js')) }}"></script>
        <script src="{{ absolute_url(asset('js/jquery-3.3.1.min.js')) }}"></script>
    </head>
    
    <body>
        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <div class="navbar-header">
                    
                   
                    <a class="navbar-brand" href="#">SF3 CRUD Application</a>
                </div>
            </div>
        </nav>
 
        <div class="container">
        <div class="row">
            {% for success_message in app.session.flashbag.get('message') %}
                <div class="alert alert-success">
                    {{ success_message }}
                </div>
            {% endfor %}        
        </div>
        </div>